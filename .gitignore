node_modules
.next
.env
.env.local
*.tsbuildinfo
next-env.d.ts

# Local Netlify folder. The feature markers under .netlify/features must be
# committed — they are what enables Identity and other primitives on deploy.
.netlify/*
!.netlify/features/
